export type CartItemType = {
  id: string;
  title: string;
  quantity: number;
  total: number;
  price: number;
};

export type ProductType = {
  id: string;
  title: string;
  price: number;
  description: string;
};
