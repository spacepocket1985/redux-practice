export type CartItemType = {
  title: string;
  quantity: number;
  total: number;
  price: number;
};

export type ProductType = {
  id: string;
  title: string;
  price: number;
  description: string;
};
